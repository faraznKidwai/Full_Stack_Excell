{
  "name": "zam-zam-screener-monolith",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.3",
    "yahoo-finance2": "^2.13.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  },
  "scripts": {
    "build": "npm run vercel-build",
"vercel-build": "prisma generate --schema=./backend/prisma/schema.prisma && npm install --prefix frontend --include=dev && npm run build --prefix frontend"
  }
}
